export type RawMessage = {
  reacjiDateTime: string;
  authorId: string;
  authorName: string;
  text: string;
  authorEmail: string;
  url?: string;
  fileUrl?: string;
  originalDateTime: string;
};
